import React from "react";
import '../styles/header.css';

const header = () => {
    return(
        <div>
            <header>
                <h1>Home</h1>
            </header>
        </div>
    );
}

export default header;
